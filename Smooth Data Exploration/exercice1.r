# Chargez le package
  library(dplyr)
  
  # Chargez le jeu de données mtcars dans votre espace de travail,Par exemple, nous pourrions choisir le jeu de données mtcars, qui contient des informations sur différentes voitures.
  data("mtcars")
  
  # Examinez les premières lignes du jeu de données
  head(mtcars)